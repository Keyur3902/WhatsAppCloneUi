import 'package:flutter/material.dart';

class CallInfoScreen extends StatelessWidget {
  const CallInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Call Info Screen'),
      ),
    );
  }
}
